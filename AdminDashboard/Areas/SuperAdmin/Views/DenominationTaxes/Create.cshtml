@model CreateDenominationTaxesViewModel
@using AdminDashboard.Models.SwaggerModels;
@{
    ViewData["Title"] = "CreateDenominationTaxes";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    <div class="card-header">
                        <h2>Create Denomination Tax</h2>
                    </div>
                    <div class="card-body">
                        <div asp-validation-summary="All" class="text-danger"></div>
                        <input type="hidden" asp-for="DenominationId" />
                        <input type="hidden" asp-for="DenominationName" />
                        <div class="form-group">
                            <label class="control-label">Tax Value <span class="validation-error">*</span></label>
                            <select asp-for="TaxesId" asp-items="Model.Taxes" class="form-control" data-style="select-with-transition" title="Choose a Tax Value" data-size="7">
                                <option disabled selected> Choose a Tax </option>
                            </select>
                            <span asp-validation-for="TaxesId" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="card-footer">
                        <input type="submit" value="Save" class="btn btn-success" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script>

    </script>
}
